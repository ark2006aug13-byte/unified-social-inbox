import { InboxWorkspace } from "@/components/inbox/inbox-workspace";

export default function AiAssistantPage() {
  return (
    <InboxWorkspace
      title="AI Assistant"
      subtitle="Use summaries, categorization, and smart replies to move faster through your highest-value conversations."
    />
  );
}
