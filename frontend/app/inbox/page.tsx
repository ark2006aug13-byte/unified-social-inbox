"use client";

import { InboxWorkspace } from "@/components/inbox/inbox-workspace";

export default function InboxPage() {
  return (
    <InboxWorkspace
      title="Unified Inbox"
      subtitle="Review Gmail plus upcoming cross-channel conversations in one workspace, search across threads, and use AI to draft faster replies."
    />
  );
}
